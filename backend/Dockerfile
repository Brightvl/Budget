# Используем официальный образ с Java 17 (или нужной вам версии)
FROM openjdk:17-jdk-alpine

# Устанавливаем рабочую директорию в контейнере
WORKDIR /app

# Копируем и собираем проект (предполагается, что сборка выполняется заранее)
COPY target/backend-0.0.1-SNAPSHOT.jar /app/app.jar

# Устанавливаем команду для запуска Spring Boot приложения
ENTRYPOINT ["java","-jar","/app/app.jar"]

# Указываем порт, на котором будет работать приложение
EXPOSE 8080
